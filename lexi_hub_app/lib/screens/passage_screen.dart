
import 'package:flutter/material.dart';

class PassageScreen extends StatelessWidget {
  const PassageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('장문 화면', style: TextStyle(fontSize: 24)),
    );
  }
}
